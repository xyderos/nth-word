#include <stdlib.h>
#include <time.h>
//hash.c
unsigned int getPrime();

unsigned int hash(unsigned int, char*);

unsigned int hashString(const unsigned int, const char* const, unsigned int, unsigned int);

